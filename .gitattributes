# Keep shell scripts and other Linux-runtime artifacts as LF in the working
# tree so they don't break when checked out on Windows with core.autocrlf=true
# and then SFTP'd to a Linux box.
*.sh text eol=lf
*.service text eol=lf
openwrt/poc-single-box/droplet-openwrt-attach text eol=lf
