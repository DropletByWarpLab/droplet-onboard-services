# python-tool

A tool extension in Python. `tool.py` exposes `run(input)`; `test_tool.py`
covers it.

```
pytest          # tests
ruff check .    # lint
```

No dependencies: the sandbox has no network.
