# pi-gen clone (toolchain — fetched by scripts/build-image.sh)
.pi-gen/

# Build artifacts
output/
build.log
*.img
*.img.gz
*.img.gz.sha256

# pi-gen working / deploy trees (in case a build runs outside .pi-gen/)
deploy/
work/
