# CPU-only torch: WARP-POC. Avoid pulling 5 GB of CUDA wheels that the
# AMD dGPU can't use anyway ? Ollama on the host handles GPU inference.
# Must be at the top + before sentence-transformers, otherwise pip's
# resolver fetches CUDA torch as a transitive dep of sentence-transformers
# before reading the per-line index override.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110.0
uvicorn>=0.29.0
litellm>=1.30.0
cryptography>=42.0.0
pydantic>=2.6.0
httpx>=0.27.0
sse-starlette>=2.0.0
redis>=5.0.0
grpcio>=1.62.0
grpcio-tools>=1.62.0
torch>=2.3.0
sentence-transformers>=3.0.0

# BGE-reranker-base int8 via optimum + onnxruntime (CPU).
onnxruntime>=1.18,<2.0
optimum>=1.21,<2.0
huggingface_hub>=0.24,<1.0
