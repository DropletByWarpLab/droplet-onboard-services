fastapi>=0.110.0
uvicorn>=0.29.0
litellm>=1.30.0
cryptography>=42.0.0
pydantic>=2.6.0
httpx>=0.27.0
sse-starlette>=2.0.0
redis>=5.0.0
grpcio>=1.62.0
grpcio-tools>=1.62.0
sentence-transformers>=3.0.0
torch --index-url https://download.pytorch.org/whl/cpu

# WARP-286: BGE-reranker-base int8 via optimum + onnxruntime.
# Used by the Rerank gRPC handler for hybrid retrieval.
#
# Do NOT also pin onnxruntime explicitly — the optimum[onnxruntime] extra
# pulls in a compatible version transitively. Newer optimum releases
# (1.27+) bumped their onnxruntime constraints past <2.0, so an explicit
# upper-bound pin here makes the resolver impossible to satisfy.
optimum[onnxruntime]>=1.21,<2.0
huggingface_hub>=0.24,<1.0
