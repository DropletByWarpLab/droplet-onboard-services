grpcio>=1.83.0
grpcio-tools>=1.82.1
cryptography>=49.0.0
click>=8.4.2
# Real backend dep — production only. Builds against libtss2-dev
# from the Dockerfile's apt-get install. The mock backend never imports
# this; the import lives inside RealBackend so dev machines without TPM
# headers can still run the mock-only test suite.
tpm2-pytss>=2.3.0; sys_platform == "linux"
