-r requirements.txt
pytest==9.1.1
# httpx backs fastapi.testclient.TestClient, which the suite drives the real
# app through — no ASGI mock anywhere.
httpx==0.28.1

# WARP-1948 — OSV floor for pytest's transitive pygments (see the matching
# floor block in requirements.txt for rationale).
pygments>=2.20.0
