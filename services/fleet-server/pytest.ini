[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    pebble: integration tests that need a live Pebble ACME server (skipped unless FLEET_PEBBLE_TESTS=1)
