fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pillow>=10.0.0
psutil>=5.9.0
luma.lcd>=2.13.0
pydantic>=2.0.0
spidev>=3.6
evdev>=1.6.1
# GPIO library for the Jetson (our target). Provides an RPi.GPIO-compatible
# API that gpio_shim.py aliases so luma.core can drive DC/RST without
# modification. Harmless on dev hosts where it just won't import.
Jetson.GPIO>=2.1.3
# PyPortal backend — USB-serial control channel to an Adafruit PyPortal
# Titano running the CircuitPython firmware in ./pyportal/.
pyserial>=3.5
