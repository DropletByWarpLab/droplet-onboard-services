"""Operator console — modules for system / container / service inspection.

Each module is single-purpose so a future fleet-management UI can pull
just the bits it needs without dragging the whole FastAPI surface.
"""
