# ops-console runtime deps. Test deps in requirements-dev.txt (matches
# the file-indexer / voice-io split convention).
fastapi==0.115.0          # HTTP layer
uvicorn[standard]==0.30.6 # ASGI server
pydantic==2.9.2           # request/response schemas
httpx==0.27.2             # fan-out probes to other services' /health
docker==7.1.0             # docker SDK — container list/logs/restart
psutil==6.0.0             # CPU/mem/disk/network/uptime stats
