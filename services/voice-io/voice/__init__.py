"""Voice-orchestrator core package — audio I/O + (in subsequent commits)
wake-word detection, STT, TTS, and the pipeline that chains them.

Hardware-compatibility design lives in `voice.devices`. Read its
docstring before adding new device types.
"""
