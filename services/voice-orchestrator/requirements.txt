# Runtime
sounddevice==0.4.7        # PortAudio binding — preferred over PyAudio for clean shutdown + numpy interop
numpy==1.26.4             # Audio buffer math
fastapi==0.115.0          # Control surface
uvicorn[standard]==0.30.6 # ASGI server for FastAPI
pydantic==2.9.2           # Schemas
httpx==0.27.2             # Async HTTP client for orchestrator/LLM proxying (used in subsequent commits)
python-multipart==0.0.10  # FastAPI form-upload support (mic-test endpoint accepts audio uploads)

# Wake / STT / TTS land in subsequent commits — declared here so the
# image build is one rebuild for the whole stack rather than three.
openwakeword==0.6.0        # Wake-word detection (Apache 2.0)
# wyoming-faster-whisper is a separate container in the compose stack
# (not bundled into this service's pip set) — keeps STT model load
# isolated from the always-on audio loop's lifecycle.

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2  # also serves as the FastAPI testclient transport
