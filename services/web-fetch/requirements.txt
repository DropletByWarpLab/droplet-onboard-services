# WARP-1948: brought up to the fastapi/uvicorn pins every sibling service uses
# (ai-gateway, routing, switch, ops-console…). The old fastapi 0.115 line caps
# starlette below 0.42, which the osv-nightly flags (PYSEC-2026-161/-1941/
# -1942/-2280/-2281/-248/-249); 0.141.1 resolves a patched starlette.
fastapi==0.141.1
uvicorn[standard]==0.52.1
# httpx is the canonical Python HTTP client across services (camera-discovery,
# routing, ops-console). All outbound calls go through providers.create_client
# with a 10 s timeout; destinations are fixed constants registered in
# docs/security/allowed-egress.yaml.
httpx==0.28.1

# WARP-1948 — OSV floor: the osv-nightly resolver scores transitive deps at
# their minimum resolvable version; state the real minimum instead of
# suppressing the advisory.
idna>=3.15
