{
  "name": "@droplet/integration-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:docker": "docker compose -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from integration-tests"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
